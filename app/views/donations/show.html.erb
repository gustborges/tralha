
<% if current_user.receiver? %>
  <div class="container">
    <div class='card-donation d-flex'>
      <div class='card-donation-infos rounded-lg d-flex justify-content-center'>
        <h3> <%= @donation.name.capitalize %> </h3>
        <% @donation.photo.attached? do %>
          <%= cl_image_tag @donation.photo.key, height: 30, width: 40, crop: :fill %>
        <% end %>
        <p> <%= @donation.description %> </p>
        <%= link_to image_tag("https://img.icons8.com/wired/64/000000/whatsapp.png", height: "32", width: "32"), "https://api.whatsapp.com/send?phone=55#{@phone}&text=Olá,%20tenho%20interesse%20na%20sua%20doação", target: "_blank" %>
       </div>
    </div>
  </div>
<% else %>
  <div class="container">
    <h1>Essa é a sua Doação</h1>
    <div class='card-donation d-flex'>
      <div class='card-donation-infos rounded-lg d-flex justify-content-center'>
        <h3> <%= @donation.name.capitalize %> </h3>
        <% @donation.photo.attached? do %>
          <%= cl_image_tag @donation.photo.key, height: 30, width: 40, crop: :fill %>
        <% end %>
        <p> <%= @donation.description %> </p>
        <div class =' d-flex justify-content-around'>
          <div>
            <h3><%= link_to donation_path(@donation),
                 method: :delete,
                 data: { confirm: "Tem certeza que quer apagá-la?" },
                 class: "" do %>
                 <i class="fas fa-trash-alt"></i>
                <% end %>
            </h3>
          </div>
          <div>
            <%= link_to edit_donation_path(@donation), class:"" do %>
            <i class="fas fa-edit"></i>
            <% end %>
          </div>
       </div>
      </div>
    </div>
    <h2>Essas são as pessoas que poderão receber sua doação.</h2>
    <h2>Logo uma delas vai enviar uma mensagem para você =)</h2>
    <div class='card-donation d-flex'>
      <% @users.each do |user| %>
        <div class='card-donation-infos rounded-lg d-flex justify-content-center'>
          <h3> <%= user.name.capitalize %> </h3>
          <p> <%= user.description %> </p>
        </div>
      <% end %>
    </div>
   </div>
<% end %>
